import 'package:flutter/material.dart';

class LibrarySavedQuestionsPage extends StatelessWidget {
  const LibrarySavedQuestionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Saved Questions'),);
  }
}
